This is a simple ping program.


Usage:
just import ppingy and use ppingy.ping(...)
you can also just use ppingy.is_alive(...)

Look at the funtion's implementation for more details on args and return results.